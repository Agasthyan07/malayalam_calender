// AdSlot is currently disabled pending AdSense approval.
export default function AdSlot({ slotId }: { slotId?: string }) {
    return null;
    /*
    return (
        <div className="w-full h-[100px] bg-gray-100 border-2 border-dashed border-gray-300 flex items-center justify-center rounded-lg my-4 text-gray-400">
            Ad Space ({slotId || 'Responsive'})
        </div>
    );
    */
}
